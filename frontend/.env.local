# Local development - can override if needed
# By default, Vite proxy forwards /api requests to localhost:5002 (see vite.config.js)
# Uncomment below if you need to set it explicitly:
# VITE_API_URL=http://localhost:5002
